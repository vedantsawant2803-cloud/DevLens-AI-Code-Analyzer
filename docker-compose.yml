services:
  devlens:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - server/.env
    environment:
      - NODE_ENV=production
      - PORT=5000
      - CLIENT_URL=http://localhost:5000
      - API_PUBLIC_URL=http://localhost:5000
    restart: unless-stopped
